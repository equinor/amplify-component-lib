{
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "cd client && yarn",
    "pretty": "cd client && yarn pretty",
    "lint": "cd client && yarn lint",
    "pretty:fix": "cd client && yarn pretty:fix",
    "lint:fix": "cd client && yarn lint:fix",
    "postinstall": "husky install",
    "prepare": "husky install",
    "precommit": "node tooling/commit-tasks.mjs"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lint-staged": "^15.2.0"
  }
}
